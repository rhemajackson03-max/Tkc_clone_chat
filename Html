<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>TKC Chat — Clone</title>

  <!-- Bootstrap CDN for quick styling -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #f8f9fa; }
    #chatBox { height: 60vh; overflow-y: auto; background: white; padding: 1rem; border-radius: 8px; }
    .msg { margin-bottom: .6rem; }
    .msg .meta { font-size: .8rem; color: #6c757d; }
    .system { color: #0d6efd; font-style: italic; }
    #roomsList { max-height: 40vh; overflow-y: auto; }
    #typingIndicator { font-size: .9rem; color: #6c757d; }
  </style>
</head>
<body>
  <div class="container py-3">
    <h2 class="mb-3">TKC Chat — Clone</h2>

    <!-- username + room selection -->
    <div id="joinCard" class="card mb-3">
      <div class="card-body">
        <div class="row g-2">
          <div class="col-md-4">
            <input id="username" class="form-control" placeholder="Enter display name" />
          </div>
          <div class="col-md-4">
            <input id="roomName" class="form-control" placeholder="Room name (or new room)" />
          </div>
          <div class="col-md-4 d-flex">
            <button id="btnJoin" class="btn btn-primary me-2">Join Room</button>
            <button id="btnCreate" class="btn btn-outline-secondary">Create Room</button>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <!-- rooms list -->
      <div class="col-md-3">
        <div class="card">
          <div class="card-header">Rooms</div>
          <div class="card-body p-2" id="roomsList">
            <div id="noRooms" class="text-muted small">No rooms yet</div>
          </div>
        </div>
      </div>

      <!-- chat area -->
      <div class="col-md-6">
        <div class="card">
          <div class="card-header d-flex justify-content-between">
            <div>
              <strong id="currentRoom">Not in a room</strong>
              <div id="typingIndicator"></div>
            </div>
            <div>
              <small id="userList" class="text-muted"></small>
            </div>
          </div>

          <div class="card-body" id="chatBox"></div>

          <div class="card-footer">
            <form id="msgForm" class="d-flex" onsubmit="return false;">
              <input id="msgInput" class="form-control me-2" placeholder="Type a message..." autocomplete="off">
              <button id="sendBtn" class="btn btn-success">Send</button>
            </form>
          </div>
        </div>
      </div>

      <!-- side info -->
      <div class="col-md-3">
        <div class="card">
          <div class="card-header">Info</div>
          <div class="card-body">
            <p class="small text-muted">This is a demo chatroom. For production use a persistent store (Redis/Postgres) and user-session mapping.</p>
            <p><strong>Actions</strong></p>
            <ul>
              <li>Enter name + room → Join</li>
              <li>Create a new room → Create Room</li>
              <li>Open multiple browser tabs to test multi-user behavior</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Socket.IO client -->
  <script src="https://cdn.socket.io/4.6.1/socket.io.min.js"></script>
  <script src="/static/chat.js"></script>
</body>
</html>
